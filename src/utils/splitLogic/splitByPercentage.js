// export function splitByPercentage(items, percentages) {
//   const totalQty = items.reduce((s, i) => s + i.quantity, 0);

//   return percentages.map(p => {
//     return items.map(item => ({
//       ...item,
//       quantity: Math.round((item.quantity * p) / 100),
//     }));
//   });
// }
