// export function splitByMembers(items, membersCount) {
//   const splits = Array.from({ length: membersCount }, () => []);

//   items.forEach((item, index) => {
//     splits[index % membersCount].push(item);
//   });

//   return splits;
// }
